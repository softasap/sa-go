---
# defaults file for sa-go

  root_dir: "{{ role_path }}"
  role_dir: "{{ role_path }}"

  go_version: 1.6

  go_path_lines:
    - "export PATH=$PATH:/usr/local/go/bin"
    - "export GOPATH=$HOME/go"
    - "export PATH=$GOPATH/bin:$PATH"
